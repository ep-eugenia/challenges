/* Calculate surface
Create a function who returns the area of a surface in m2. Return a string like "The area is 'your number here' m2"
--------------------------------------------------------------------------------------------------------------------*/
function calculateSurface(length, width) {

  let result = (length * width);

console.log("The area is " + result + " m2");
} 
calculateSurface(20, 10); //output: 200m2